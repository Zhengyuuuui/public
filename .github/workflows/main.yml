name: CS2饰品每小时监控
on:
  schedule:
    # 已做时区转换，精准对应北京时间每小时整点执行
    - cron: '0 16-23,0-15 * * *'
  workflow_dispatch: # 保留手动触发按钮，方便测试

jobs:
  trigger-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: 触发Coze工作流
        run: |
          curl --location --request POST 'https://api.coze.cn/api/v1/workflow/run_webhook?token=cztei_l2x9RCjgpP2HyGg44PDRbfs7RXo0agYDBOv0v6LFCMP41tWszHCyMCn9Ohp3vbkoT' \
          --header 'Content-Type: application/json' \
          --data-raw '{}' \
          --max-time 30 \
          -v
